language: r
cache: packages

r:
  - release
  - oldrel
  - devel

sudo: false

addons:
  apt:
    packages:
      - qpdf

r_packages:
  - vegan
  - stats
  - sampling

before_install:
  - tlmgr install sidecap

warnings_are_errors: false

r_build_args: " --compact-vignettes=gs+qpdf"
